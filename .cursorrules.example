# DroidMorning - Cursor AI Rules

## 🌐 언어 설정
- **항상 한국어로 답변**해주세요
- 코드 주석과 문서는 한국어로 작성합니다
- 변수명, 함수명 등은 영어로 작성합니다

## 📦 프로젝트 정보
- **프로젝트 유형**: Kotlin Multiplatform (KMP)
- **타겟 플랫폼**: Android, iOS
- **아키텍처**: Clean Architecture (Domain, Data, Presentation)
- **DI**: Koin 4.1.1
- **UI**: Compose Multiplatform
- **빌드 도구**: Gradle (Kotlin DSL)

## 🎯 코드 스타일
- **Kotlin 스타일**: ktlint 사용 (`.editorconfig` 참조)
- **커밋 전 자동 검사**: `./gradlew ktlintCheck`
- **자동 포맷**: `./gradlew ktlintFormat`

## 📝 커밋 컨벤션
**Angular JS Commit Convention을 따릅니다:**

### 커밋 메시지 형식
```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 종류
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `docs`: 문서 수정
- `style`: 코드 포맷팅, 세미콜론 누락 등 (코드 변경 없음)
- `refactor`: 코드 리팩토링
- `test`: 테스트 코드 추가/수정
- `chore`: 빌드 업무, 패키지 매니저 수정 등

### Scope 예시
- `android`: Android 플랫폼 관련
- `ios`: iOS 플랫폼 관련
- `domain`: Domain 레이어
- `data`: Data 레이어
- `presentation`: Presentation 레이어
- `auth`: 인증 관련
- `network`: 네트워크 관련
- `di`: 의존성 주입 관련

### 커밋 메시지 예제
```
feat(auth): Google 로그인 기능 구현

- Supabase Auth를 사용한 Google OAuth 연동
- Android는 Credential Manager 사용
- iOS는 GoogleSignIn SDK 사용

Closes #15
```

## 🐛 이슈 생성 규칙

**이슈 생성 시 `.github/ISSUE_TEMPLATE/issue-template.md`를 따릅니다:**

### 이슈 생성 체크리스트
1. 명확한 제목 작성 (예: "Google 로그인 기능 구현")
2. Description에 작업 배경과 목적 명시
3. TODO 리스트를 체크박스로 작성
4. 적절한 라벨 추가 (feature, bug, enhancement 등)
5. 필요시 assignee 지정

### 이슈 템플릿 구조
```markdown
## Description
> 어떤 작업에 대한 이슈인지 적어주세요. ex) 프로젝트 초기 세팅

## TODO
> 이슈에서 해야 할 일을 적어주세요.
- [ ] 할일 1
- [ ] 할일 2
- [ ] 할일 3
```

### 이슈 작성 예제
```markdown
## Description
> Google 소셜 로그인 기능을 구현합니다. Android는 Credential Manager를, iOS는 GoogleSignIn SDK를 사용합니다.

## TODO
- [ ] Supabase Auth 설정
- [ ] Android Google 로그인 구현
- [ ] iOS Google 로그인 구현
- [ ] 로그인 UI 작성
- [ ] 테스트 코드 작성
```

## 🔀 PR 생성 규칙

**PR 생성 시 `.github/PULL_REQUEST_TEMPLATE.md`를 따릅니다:**

### PR 생성 체크리스트
1. 브랜치명은 이슈 번호 포함 (예: `feature/15-google-login`)
2. 이슈 번호 명시 필수
3. 작업 내용 상세히 설명
4. 결과물 스크린샷/영상/GIF 첨부 (UI 작업인 경우 필수)
5. PR 생성 전 `ktlintCheck` 통과 확인
6. 충돌(conflict) 해결 후 생성

### PR 템플릿 구조
```markdown
## 이슈 번호
Closes #이슈번호

## 작업내용
작업한 내용을 설명해주세요. 왜 수정했는지? 무엇을 구현했는지 등등

## 결과물
구현한 것을 스크린샷, 영상 또는 GIF 형태로 올려주세요.

| Before | After |
|:--:|:--:|
| (이미지) | (이미지) |
```

### PR 작성 예제
```markdown
## 이슈 번호
Closes #15

## 작업내용
Google 소셜 로그인 기능을 구현했습니다.

**구현 내용:**
- Supabase Auth를 사용한 Google OAuth 연동
- Android: Credential Manager API 사용
- iOS: GoogleSignIn SDK 사용
- 공통 인터페이스로 플랫폼별 구현 추상화

**변경 파일:**
- `data/src/commonMain/kotlin/auth/AuthRepository.kt`
- `data/src/androidMain/kotlin/auth/AuthRepositoryImpl.kt`
- `data/src/iosMain/kotlin/auth/AuthRepositoryImpl.kt`
- `presentation/src/commonMain/kotlin/login/LoginScreen.kt`

## 결과물
| Android | iOS |
|:--:|:--:|
| ![android_login](링크) | ![ios_login](링크) |

[로그인 시연 영상](링크)
```

## 🚀 PR 생성 명령어 가이드

**GitHub CLI를 사용한 PR 생성:**
```bash
# 1. 브랜치 생성 (이슈 번호 포함)
git checkout -b feature/15-google-login

# 2. 작업 완료 후 커밋
git add .
git commit -m "feat(auth): Google 로그인 기능 구현"

# 3. 푸시
git push origin feature/15-google-login

# 4. PR 생성 (gh CLI 사용)
gh pr create --title "feat(auth): Google 로그인 기능 구현" \
  --body "$(cat <<'EOF'
## 이슈 번호
Closes #15

## 작업내용
...

## 결과물
...
EOF
)"
```

## 🔍 코드 리뷰 시 확인사항
- [ ] ktlint 통과 여부
- [ ] 테스트 코드 작성 여부
- [ ] Android/iOS 양쪽 플랫폼 동작 확인
- [ ] Clean Architecture 레이어 준수
- [ ] Koin 의존성 주입 올바른 사용
- [ ] 불필요한 플랫폼별 분기 최소화
- [ ] 공통 코드 최대한 활용

## 📚 프로젝트 가이드 문서
- [Koin Setup Guide](./docs/KOIN_SETUP_GUIDE.md)
- [Google Login Guide](./docs/GOOGLE_LOGIN_GUIDE.md)
- [CocoaPods Setup Guide](./docs/COCOAPODS_SETUP_GUIDE.md)

## 🛠️ 개발 전 체크리스트
- [ ] 최신 `main` 브랜치와 동기화
- [ ] 이슈 생성 및 번호 확인
- [ ] 적절한 브랜치명으로 브랜치 생성
- [ ] 개발 환경 설정 확인 (Gradle sync, CocoaPods 등)

## 🧪 테스트 규칙
- 새로운 기능 추가 시 단위 테스트 작성 필수
- Repository, UseCase 레이어는 테스트 커버리지 우선
- 테스트 명명: `메서드명_시나리오_예상결과` 형식
  ```kotlin
  @Test
  fun login_withValidCredentials_returnsSuccess() { }
  ```

## 🔧 AI 사용 시 주의사항
- 이슈나 PR 생성 요청 시 **반드시 위 템플릿 형식 준수**
- 커밋 메시지는 **Angular JS Convention 준수**
- 생성된 코드는 **ktlint 자동 포맷 적용**
- KMP 프로젝트 특성상 **플랫폼별 구현 확인 필요**
